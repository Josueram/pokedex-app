import React from "react";
import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Favorite = () => {
  return (
    <SafeAreaView>
      <Text>Estamos en la pantalla de Pokémons Favoritos</Text>
    </SafeAreaView>
  );
};

export default Favorite;
