import React from "react";
import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Pokemon = () => {
  return (
    <SafeAreaView>
      <Text>Estamos en la pantalla Pokémon</Text>
    </SafeAreaView>
  );
};

export default Pokemon;
